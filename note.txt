# Example environment variables for local Packer builds.
# Do not store real secrets in git.
export PROXMOX_URL="https://<PVE-IP>:8006/api2/json"
export PROXMOX_USERNAME="root@pam!packer"
export PROXMOX_TOKEN="<REDACTED>"
export PROXMOX_NODE="homelab"
export PACKER_ISO_STORAGE="hdd-data"
export PACKER_BRIDGE_LAN="vmbr0"
export PACKER_SSH_PUBLIC_KEY="ssh-ed25519 AAAA... user@host"
export PACKER_SSH_PRIVATE_KEY="C:\\path\\to\\packer_id_ed25519"

# Example vars file:
# variables.auto.pkrvars.hcl.example
